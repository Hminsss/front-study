// 변수 let
let value = 1;
console.log(value);

// 상수 const 값 변경 불가
const a = 1;

// 이전엔 많이 사용했지만 지금은 let을 많이 쓴다. 이제 안써서 몰라도 됨
var b = 1;

let num = 1; // 정수
let str1 = "a"; // 문자열
let str2 = 'a'// 세미콜론도 없어도 됨

let good = true; // boolean
let loading = false;

// 앖다의 의미
let n = null; // 없다 
let s = undefined; //정해지지 않았다.